export const POLL_INTERVALS = {
  CPU_LOAD: 500,
  CPU_TEMP: 2000,
  CPU_SPEED: 1000,
  CPU_TASKS: 5000,
  FILESYSTEM: 1000,
  FILESYSTEM_RETRY: 1000,
  RAM: 1500,
  UPTIME: 60000,
  BATTERY: 3000,
	HARDWARE_INSPECTOR: 20000,
};

export const THROTTLE = {
  SOUND_FX: 30,
  TERMINAL_REFIT: 10000,
};

export const RAM_POINT_COUNT = 440;

export const UI_TIMING = {
	KEY_HOLD_INTERVAL: 70,
	KEY_HOLD_TIMEOUT: 400,
	KEY_BLINK_DURATION: 100,
	MODAL_DRAG_DELAY: 500,
	MODAL_CLOSE_ANIMATION: 100,
};

export const Z_INDEX = {
	ERROR_MODAL: 1500,
	WARNING_MODAL: 1000,
	INFO_MODAL: 500,
};

export const TERMINAL_DEFAULTS = {
	FONT_SIZE: 15,
	SCROLLBACK: 1500,
};

export const AUDIO_DEFAULTS = {
	VOLUME: 0.4,
};
